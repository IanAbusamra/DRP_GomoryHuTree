#include <bits/stdc++.h>
using namespace std;

/** 
 * Given an undirected weighted graph, returns a tree such
 * that the max flow between any two vertices equals the
 * minimum weight of an edge on the unique path of the tree
 * between the two vertices
 *  */
vector<tuple<int, int, int>> gomory_hu(vector<tuple<int, int, int>> graph) {
    vector<tuple<int, int, int>> tree;
    return tree;
}